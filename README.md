# TestProjectDocker101
Test Project for Docker and Playwright

```cmd
docker compose up --build
```

Access via:
- Web Browser: `http://localhost:6080/vnc.html`
- VNC Client: `vnc://localhost:5900`

Open terminal session and enter:

```cmd
npx playwright open google.com
```
